# 2026-05-16 작업 재개 컨텍스트

## 현재 목표

앱 완성도를 우선 올린다. 출시 준비보다 먼저 `docs/Today wear-3`에 있는 Claude 디자인 시안을 Flutter 앱에 단계적으로 반영한다.

2026-05-17 기준으로 `docs/Today wear-3`의 디자인 시안은 단순 polish가 아니라 **제품 스펙 변경**으로 취급한다. 화면 스타일뿐 아니라 저장 플로우, 기록 필터, 캘린더/리포트, OOTD 핀 태그처럼 새 기능으로 이어지는 항목도 개발 범위에 포함한다.

## 오늘 완료한 작업

### #M05a 캐릭터 위젯 개선

- 파일: `lib/presentation/widgets/character_widget.dart`
- 변경 내용:
  - 캐릭터 에셋을 `WeatherSnapshot.temperatureBand` 기반으로 우선 로드한다.
  - 예: `assets/characters/female_band3.png`
  - 아직 18개 band 에셋이 없으므로 기존 이미지로 fallback한다.
  - fallback 순서: `bandN/rainy` 에셋 실패 → 기존 `sunny/cloudy/cold/rainy` 에셋 → 기본 사람 placeholder
- 검증:
  - `dart format lib/presentation/widgets/character_widget.dart`
  - `flutter test` 통과, 61개 테스트 성공

### #M06 네이티브 스플래시 구성

- 추가 패키지:
  - `flutter_native_splash: ^2.4.7`
- 추가/변경:
  - `pubspec.yaml`에 `flutter_native_splash` 설정 추가
  - `assets/images/splash_icon.png` 추가
  - iOS/Android native splash 리소스 생성
- 실행한 명령:
  - `flutter pub add --dev flutter_native_splash`
  - `dart run flutter_native_splash:create`
  - `flutter test`
  - `flutter build ios --simulator`
- 검증:
  - `flutter test` 통과
  - `flutter build ios --simulator` 성공
  - 결과: `build/ios/iphonesimulator/Runner.app`

## 실기기 빌드 상태

디바이스 인식됨:

```text
Kyeongjinku (mobile) • 00008110-001E05C00212801E • ios • iOS 18.5 22F76
```

디버그 실행:

```bash
flutter run -d 00008110-001E05C00212801E
```

- Xcode build 성공
- 설치/실행 성공
- Dart VM Service 출력됨
- 이후 로컬 콘솔 프로세스를 정리하는 과정에서 디버그 세션 앱도 SIGTERM으로 멈춤

릴리즈 실행:

```bash
flutter run --release -d 00008110-001E05C00212801E
```

- Xcode build 성공
- `Installing and launching...` 단계에서 오래 대기
- 프로세스 정리 후 Flutter 출력:

```text
Could not run build/ios/iphoneos/Runner.app on 00008110-001E05C00212801E.
Try launching Xcode and selecting "Product > Run" to fix the problem:
  open ios/Runner.xcworkspace
```

다음 실기기 확인 방법:

1. Xcode에서 `ios/Runner.xcworkspace` 열기
2. 타겟 디바이스를 `Kyeongjinku`로 선택
3. `Product > Run`
4. 신뢰/서명/프로비저닝 문제가 있으면 Xcode 안내대로 처리
5. 이후 다시 터미널에서 아래 명령 재시도

```bash
flutter run --release -d 00008110-001E05C00212801E
```

## 중요한 경고

iOS 시뮬레이터/실기기 빌드 중 다음 경고가 반복적으로 출력됨:

```text
The following target(s) do not support arm64 architecture,
which is a requirement for Apple Silicon iOS 26+ simulators
```

- `camera_avfoundation`, `geolocator_apple`, `google_mlkit_selfie_segmentation`, `image_cropper`, `image_picker_ios`, `sqflite_darwin` 등 플러그인 관련 경고
- 현재 iOS 시뮬레이터 빌드와 디버그 실기기 빌드는 성공했으므로 즉시 blocker는 아님
- 최신 iOS 26 시뮬레이터 실행 문제가 생기면 플러그인 업데이트/Pod 설정을 봐야 함

## Claude 디자인 반영 기준

디자인 문서 위치:

```text
docs/Today wear-3
```

핵심 파일:

- `docs/Today wear-3/UX Spec.html`
- `docs/Today wear-3/screens-home.jsx`
- `docs/Today wear-3/screens-add.jsx`
- `docs/Today wear-3/screens-closet.jsx`
- `docs/Today wear-3/screens-history.jsx`
- `docs/Today wear-3/shared.jsx`
- `docs/Today wear-3/styles.css`

디자인 핵심:

- 라벤더 기반 톤
  - primary: `#8a7ddc`
  - primary dark: `#7366c8`
  - bg: `#f6f3fc`
  - ink: `#2c2848`
  - muted: `#9b96b0`
  - line: `#ede9f8`
- 감성적이지만 기능 중심인 날씨 기반 코디 앱
- 5탭 구조 제안:
  - 홈
  - 옷장
  - +
  - 기록
  - 나
- 현재 Flutter 앱은 3탭:
  - 옷장
  - 오늘
  - 기록

## 디자인 반영 계획

### 0. 2026-05-17 반영 완료

1차로 Claude 디자인의 주요 화면 방향을 Flutter 앱에 반영했다.

변경 파일:

- `lib/presentation/theme/app_theme.dart`
- `lib/presentation/screens/shell_screen.dart`
- `lib/presentation/screens/today_outfit/today_outfit_screen.dart`
- `lib/presentation/widgets/collage_preview.dart`
- `lib/presentation/screens/closet/closet_screen.dart`
- `lib/presentation/screens/add_garment/add_garment_screen.dart`
- `lib/presentation/screens/add_garment/camera_guide_screen.dart`
- `lib/presentation/screens/add_garment/cutout_editor_screen.dart`
- `lib/presentation/screens/history/history_screen.dart`

완료 내용:

- 라벤더 기반 디자인 토큰 적용
- 하단 탭을 `홈 / 옷장 / + / 기록` 형태로 변경
- 홈을 `HomeB` 콜라주 중심 구조로 개편
- 옷장을 `ClosetA/D` 기반의 헤더, 검색, 필터, 3열 그리드, 빈 상태로 개편
- 옷 등록을 `AddA/B` 기반 촬영 유도 + “맞나요?” 확인 플로우로 개편
- 카메라 가이드를 어두운 viewfinder 스타일로 개편
- 컷아웃 보정 화면을 `AddC` 기반 보정 카드/툴바/완료 버튼 구조로 개편
- 기록 화면을 `HistB` 기반 2열 룩북 그리드로 개편
- 기록 화면 필터를 실제 저장 코디 데이터에 연결
- 기록 화면에 `HistA` 기반 월간 캘린더 뷰 추가
  - `룩북 / 캘린더` 전환
  - 월 이동
  - 날짜별 코디 썸네일
  - 오늘 날짜 강조
- 기록 화면에 `HistD` 기반 리포트/인사이트 뷰 추가
  - 월간 기록 횟수
  - 가장 많이 입은 옷
  - 오래 안 입은 옷
  - 저장한 날 평균 기온
- 홈 추천 코디에 `SaveC` 기반 “이대로 입었어요” 저장 플로우 추가
  - 추천 코디 미리보기
  - 한 줄 메모
  - 만족도 피드백
  - 추천 옷 자동 연결 저장
- `SaveB` 기반 오늘 입은 옷 옷장 멀티 셀렉트 저장 추가
  - 옷장 그리드에서 여러 벌 선택
  - 날씨/날짜 포함 저장
  - 선택된 옷으로 콜라주 생성
- `SaveA` 기반 OOTD 사진 핀 태그 저장 플로우 추가
  - OOTD 사진 선택/촬영
  - 사진 탭 위치에 핀 추가
  - 핀별 옷장 아이템 매칭
  - 사진을 기록 이미지로 저장
- `AddD` 기반 여러 벌 한번에 등록 추가
  - 앨범에서 여러 이미지 선택
  - 선택 이미지 그리드
  - 카테고리 빠른 지정
  - 순차 배경 제거/썸네일 생성 후 일괄 저장
- 옷장 검색 실제 동작 연결
  - 이름/카테고리/색상/스타일/시즌/메모 검색
  - 카테고리 필터와 검색어 동시 적용
- 옷장 분석 화면 추가
  - 컬러 톤 스와치/개수
  - 카테고리 구성 비율
  - 옷별 착용 횟수
  - 마지막 착용일
- `나` 탭/설정 화면 추가
  - 하단 탭 5개 구조 완료: 홈 / 옷장 / + / 기록 / 나
  - 성별/캐릭터 설정 저장
  - 단위/스타일/위치 설정 상태 표시

검증:

- `dart format`
- `flutter test` 통과
- `flutter run -d 00008110-001E05C00212801E` 실기기 배포 성공

주의:

- iOS 실기기 실행을 위해 Xcode `iOS 26.5 Simulator` component를 설치했고, DDI mount를 `xcrun devicectl device info ddiServices --auto-mount-ddis`로 정상화했다.
- `Target native_assets required define SdkRoot but it was not provided` 메시지는 출력되지만 현재 실행 blocker는 아니다.

### 1. 디자인 토큰/테마 통합

먼저 공통 테마부터 만든다.

추천 파일:

- 새 파일: `lib/core/theme/app_theme.dart`
- 수정: `lib/main.dart`

할 일:

- Claude 디자인 색상을 Flutter `ColorScheme` 또는 상수로 정리
- Scaffold 배경을 `#f6f3fc` 계열로 통일
- 카드, 칩, 버튼, bottom nav 스타일 기준 잡기
- 현재 앱의 `#6C5CE7` 중심 색을 `#8a7ddc` 중심으로 이동

### 2. 하단 탭 구조 변경

추천 파일:

- `lib/presentation/screens/shell_screen.dart`
- `lib/app_router.dart`

할 일:

- 디자인은 5탭이지만 지금은 화면이 없으므로 1차는 4탭 권장:
  - 홈
  - 옷장
  - +
  - 기록
- `+` 탭은 route branch가 아니라 `/add-garment`로 push
- `나` 탭은 설정/프로필 화면이 생긴 뒤 2차로 추가
- 현재 initial route `/today`는 유지 가능

### 3. 홈 화면 개편

기준 디자인:

- `docs/Today wear-3/screens-home.jsx`
- 우선 반영안: `HomeB` 중심 + `HomeA` 캐릭터 fallback 일부

추천 파일:

- `lib/presentation/screens/today_outfit/today_outfit_screen.dart`
- `lib/presentation/widgets/collage_preview.dart`
- `lib/presentation/widgets/character_widget.dart`

할 일:

- 상단 날씨를 큰 gradient bar가 아니라 디자인의 조용한 날씨/오늘 코디 header로 변경
- 추천 콜라주 카드를 중심으로 배치
- 버튼 문구:
  - 저장
  - 다시
  - 필요하면 더보기
- 옷장이 비었거나 추천 후보가 없을 때 캐릭터를 fallback 경험으로 사용

### 4. 옷장 화면 polish

기준 디자인:

- `docs/Today wear-3/screens-closet.jsx`

추천 파일:

- `lib/presentation/screens/closet/closet_screen.dart`

할 일:

- 기본 AppBar 느낌 줄이기
- 라벤더 배경 + 흰 카드 그리드
- 필터 칩 스타일 디자인 토큰에 맞춤
- 빈 상태 CTA를 더 명확하게:
  - “첫 옷을 등록해 추천을 시작해요”
  - 버튼: “옷 등록”

### 5. 옷 등록 플로우 polish

기준 디자인:

- `docs/Today wear-3/screens-add.jsx`
- `AddA`: 촬영 가이드
- `AddB`: AI 추론 결과 확인
- `AddC`: 배경제거 보정

추천 파일:

- `lib/presentation/screens/add_garment/camera_guide_screen.dart`
- `lib/presentation/screens/add_garment/add_garment_screen.dart`
- `lib/presentation/screens/add_garment/cutout_editor_screen.dart`

할 일:

- 촬영 가이드 화면을 어두운 viewfinder 스타일로 개선
- 등록 확인 화면을 “맞나요?” 확인 플로우로 정리
- 카테고리/색상/보온도/태그를 한 화면에서 빠르게 수정

### 6. 기록 화면 polish

기준 디자인:

- `docs/Today wear-3/screens-history.jsx`
- `docs/Today wear-3/screens-save.jsx`

추천 파일:

- `lib/presentation/screens/history/history_screen.dart`

할 일:

- 현재 리스트 카드에서 룩북 카드 형태로 개선
- 날짜, 날씨, 콜라주, 아이템 태그가 한눈에 보이게 정리
- 캘린더형 기록은 2차 작업으로 미룸

### 7. Claude 스펙 변경분: 개발 포함 항목

아래는 단순 시각 polish가 아니라 신규/확장 기능으로 개발 범위에 포함한다.

#### 기록/저장 플로우

기준 디자인:

- `docs/Today wear-3/screens-history.jsx`
- `docs/Today wear-3/screens-save.jsx`

할 일:

- 기록 필터 실제 동작 연결:
  - 전체
  - 맑음
  - 흐림
  - 비
  - 15° 이하
- 월간 캘린더 뷰 추가 (`HistA`)
  - 날짜별 저장 코디 썸네일 표시
  - 오늘 날짜 강조
  - 월 이동
- 리포트/인사이트 뷰 추가 (`HistD`)
  - 월간 기록 횟수
  - 가장 많이 입은 옷
  - 오래 안 입은 옷
- 코디 저장 플로우 확장 (`screens-save.jsx`)
  - 추천 코디에서 “이대로 입었어요” 저장 (`SaveC`)
  - 오늘 입은 옷을 옷장에서 멀티 셀렉트 (`SaveB`)
  - OOTD 사진/거울샷에 옷 핀 태그 (`SaveA`)는 2차
  - 빠른 기록/자동 매칭 (`SaveD`)은 2차

#### 옷 등록 확장

기준 디자인:

- `docs/Today wear-3/screens-add.jsx`

할 일:

- 여러 벌 한번에 등록 (`AddD`)
  - 여러 이미지 선택
  - 선택된 항목 그리드
  - N벌 저장
- AI 추론 결과의 실제 자동 태깅 강화
  - 카테고리
  - 대표 색상 이름
  - 보온도
  - 스타일 태그
  - 방수 여부

#### 옷장 확장

기준 디자인:

- `docs/Today wear-3/screens-closet.jsx`

할 일:

- 검색 실제 동작 연결
- 컬러 톤/워드로브 분석 (`ClosetB`)
- 착용 빈도/마지막 착용일 기반 매거진 리스트 (`ClosetC`)
- 빈 옷장 opt-out “캐릭터 추천만 쓸게요” 동작 정의

#### 나 탭/프로필

기준 디자인:

- `docs/Today wear-3/shared.jsx`의 5탭 구조

할 일:

- `나` 탭 추가 여부 결정
- 설정/프로필 화면 생성
- 성별/캐릭터/지역/단위/개인화 설정 이동

## 다음에 바로 시작할 작업

다음 세션에서는 아래 순서로 시작하면 된다.

```text
1. Phase D 전체 화면 실기기 스모크 테스트
2. App Store 준비 작업 재개: `#M02` API 키 --dart-define 이전
3. `#M03` unawaited_futures 경고 수정
```

## 워크트리 주의

현재 repo에는 기존부터 많은 변경사항이 있다. 전부 이번 작업에서 만든 것이 아니다.

이번 작업에서 직접 관련 있는 파일:

- `lib/presentation/widgets/character_widget.dart`
- `pubspec.yaml`
- `pubspec.lock`
- `assets/images/splash_icon.png`
- `ios/Runner/Base.lproj/LaunchScreen.storyboard`
- `ios/Runner/Info.plist`
- `ios/Runner/Assets.xcassets/LaunchImage.imageset/*`
- `ios/Runner/Assets.xcassets/LaunchBackground.imageset/*`
- `android/app/src/main/res/**` 중 splash 관련 생성 파일
- `tasks/backlog.md`
- `tasks/daily/2026-05-16.md`
- `tasks/context-2026-05-16.md`

주의:

- 사용자/기존 변경을 되돌리지 말 것
- `git reset --hard`, `git checkout --` 사용 금지
- 작업 전 관련 파일만 diff 확인
